# app/

Your actual LLM-powered application code lives here (the thing being deployed —
API endpoints, RAG pipeline, agent logic, etc.). This repo only contains the
CI/CD pipeline around it; this reference implementation ships this directory
empty on purpose. Replace this file once real app code lands here.
